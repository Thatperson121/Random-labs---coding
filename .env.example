# API Configuration
# Replace these values with your actual API keys and URLs

# API Key for external services
VITE_API_KEY=your_api_key_here

# API URL for backend services
VITE_API_URL=https://api.example.com

# Storage prefix for local storage keys
VITE_STORAGE_PREFIX=codecollab

# OpenAI API Key - IMPORTANT: DO NOT use VITE_ prefix for sensitive keys
# This will be used by Netlify functions but NOT exposed to the browser
OPENAI_API_KEY=your_openai_api_key_here

# Non-sensitive configuration for AI features (safe to expose to browser)
VITE_AI_FEATURES_ENABLED=true

# Note: Files with actual values (.env, .env.local, .env.production) should never be committed to version control
# This example file shows the required variables but doesn't contain sensitive information 